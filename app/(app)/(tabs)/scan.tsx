// app/(app)/(tabs)/scan.tsx
import { View, Text } from 'react-native';

export default function ScanScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Scan Screen — Week 3</Text>
    </View>
  );
}